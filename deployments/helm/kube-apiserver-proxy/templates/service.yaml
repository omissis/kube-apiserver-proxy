apiVersion: v1
kind: Service
metadata:
  name: {{ include "kube-apiserver-proxy.fullname" . }}
  labels:
    {{- include "kube-apiserver-proxy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kube-apiserver-proxy.selectorLabels" . | nindent 4 }}
